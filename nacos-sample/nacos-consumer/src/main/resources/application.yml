spring:
  application:
    name: nacos-consumer
server:
  port: 9280

demo:
  service:
    version: 1.0.0

nacos:
  host: 127.0.0.1
  port: 8848
  username: nacos
  password: nacos

dubbo:
  # Dubbo Protocol
  protocol:
    name: dubbo
    port: 1
  ## Dubbo Registry
  registry:
    address: nacos://${nacos.host}:${nacos.port}/?username=${nacos.username}&password=${nacos.password}